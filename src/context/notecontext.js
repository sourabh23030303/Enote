import { createContext } from "react";

const noteContext= createContext();//new context is created which holds all state related to notes

export default noteContext;


//yha sirf context api ko use kra hai react se context bnane k